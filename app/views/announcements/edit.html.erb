<div class="app-page space-y-6 py-4">
  <%= section_header \
        title: "Edit announcement",
        eyebrow: "Announcements",
        meta_items: ["announcement ##{@announcement.id}", @announcement.title] do %>
    <%= link_to "Back", @announcement, class: "btn btn-ghost btn-sm" %>
  <% end %>

  <%= render "form", announcement: @announcement %>
</div>
