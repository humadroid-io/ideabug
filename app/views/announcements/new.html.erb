<div class="app-page space-y-6 py-4">
  <%= section_header title: "New announcement", eyebrow: "Announcements", meta_items: ["editor"] do %>
    <%= link_to "Back", :announcements, class: "btn btn-ghost btn-sm" %>
  <% end %>

  <%= render "form", announcement: @announcement %>
</div>
