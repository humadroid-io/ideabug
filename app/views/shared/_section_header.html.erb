<% title_class = local_assigns[:title_class] %>
<% meta_items = Array(local_assigns[:meta_items]).compact_blank %>
<% actions = local_assigns[:actions] %>

<header class="flex flex-col gap-4 sm:flex-row sm:items-start sm:justify-between">
  <div class="min-w-0">
    <p class="app-kicker"><%= eyebrow %></p>
    <h1 class="<%= ["app-title", "mt-2", title_class].compact.join(" ") %>"><%= title %></h1>

    <% if meta_items.any? %>
      <div class="mt-3 flex flex-wrap gap-x-4 gap-y-2">
        <% meta_items.each do |item| %>
          <span class="app-meta"><%= item %></span>
        <% end %>
      </div>
    <% end %>
  </div>

  <% if actions.present? %>
    <div class="flex shrink-0 flex-wrap items-center gap-2">
      <%= actions %>
    </div>
  <% end %>
</header>
